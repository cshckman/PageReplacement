So the build instructions are as follows.
$ gcc -o pr pagereplacement.c

This was tested on a linux OS.

This creates a simulated queue and for a page replacement program. It also simulates a data structure that overwrites the least recently used data set.

./pr [numaccesses]
